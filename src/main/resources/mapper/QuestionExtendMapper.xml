<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.cyd.mapper.QuestionExtendMapper">
  <resultMap id="BaseResultMap" type="com.community.cyd.model.Question">   <!--绑定mapper接口-->
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Nov 18 14:14:37 CST 2019.
    -->
    <id column="ID" jdbcType="INTEGER" property="id" />
    <result column="TITLE" jdbcType="VARCHAR" property="title" />
    <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
    <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified" />
    <result column="CREATOR" jdbcType="INTEGER" property="creator" />
    <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
    <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
    <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
    <result column="TAG" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="com.community.cyd.model.Question">
    <result column="DESCRIPTION" jdbcType="CLOB" property="description" />
  </resultMap>

  <!--id对应dao接口中的方法名，parameterType对应dao接口中方法的参数类型-->
  <!--浏览数自增-->
  <update id="incViewCount" parameterType="com.community.cyd.model.Question">
    update QUESTION
    set VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
    where ID = #{id,jdbcType=INTEGER}
  </update>

  <!--评论数自增-->
  <update id="incCommentCount" parameterType="com.community.cyd.model.Question">
    update QUESTION
    set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where ID = #{id,jdbcType=INTEGER}
  </update>

  <!--通过tag找关联问题-->
  <select id="selectRelatedByTag" parameterType="com.community.cyd.model.Question" resultMap="BaseResultMap">
    select * from QUESTION where id != #{id} and tag regexp #{tag}
  </select>

  <!--通过search找符合条件问题个数-->
  <select id="countBySearch" parameterType="com.community.cyd.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from QUESTION
    <where>
        <if test="search != null">
         and title regexp #{search}
        </if>
     </where>
  </select>

  <!--通过search找符合条件问题-->
  <select id="selectBySearch" parameterType="com.community.cyd.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from QUESTION
    <where>
      <if test="search != null">
        and title regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>
</mapper>